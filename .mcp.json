{
  "mcpServers": {
    "google-workspace": {
      "command": "npx",
      "args": ["@anthropic/mcp-google-workspace"],
      "env": {
        "GOOGLE_CREDENTIALS_PATH": "${GOOGLE_CREDENTIALS_PATH}",
        "GOOGLE_TOKEN_PATH": "${GOOGLE_TOKEN_PATH:-./tokens/google-token.json}"
      }
    },
    "google-sheets": {
      "command": "npx",
      "args": ["@anthropic/mcp-google-sheets"],
      "env": {
        "GOOGLE_CREDENTIALS_PATH": "${GOOGLE_CREDENTIALS_PATH}",
        "GOOGLE_TOKEN_PATH": "${GOOGLE_TOKEN_PATH:-./tokens/google-token.json}",
        "METRICS_SPREADSHEET_ID": "${METRICS_SPREADSHEET_ID}"
      }
    },
    "jira": {
      "command": "npx",
      "args": ["@anthropic/mcp-jira"],
      "env": {
        "JIRA_HOST": "${JIRA_HOST}",
        "JIRA_EMAIL": "${JIRA_EMAIL}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    },
    "asana": {
      "command": "npx",
      "args": ["@anthropic/mcp-asana"],
      "env": {
        "ASANA_ACCESS_TOKEN": "${ASANA_ACCESS_TOKEN}",
        "ASANA_WORKSPACE_GID": "${ASANA_WORKSPACE_GID}"
      }
    },
    "filesystem": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-filesystem", "./graph", "./logs", "./data"],
      "env": {}
    },
    "memory": {
      "command": "npx",
      "args": ["@anthropic/claude-mem", "--db", "./graph/memory.db"],
      "env": {}
    }
  }
}
